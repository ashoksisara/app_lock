name: app_lock
description: "Multi-Profile App Locker — two profiles, one device, separate PINs."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  path: ^1.9.0
  sqflite: ^2.4.2
  shared_preferences: ^2.5.4
  crypto: ^3.0.7
  local_auth: ^3.0.0
  flutter_riverpod: ^3.2.1
  installed_apps: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
