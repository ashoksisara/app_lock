// Centralized color constants used across the app UI
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color seedColor = Color(0xFF6750A4);

  static final Color statusActive = Colors.green.shade400;
  static final Color statusInactive = Colors.red.shade400;
}
